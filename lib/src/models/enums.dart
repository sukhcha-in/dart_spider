enum ApplyMethod { urldecode, mapToList }

enum HttpResponseType { json, html }

enum UserAgentDevice { mobile, desktop }

enum Extract { product, price }

enum ParserType {
  element,
  text,
  image,
  attribute,
  json,
  url,
  http,
  strBetween,
  jsonld,
  table,
  sibling,
  urlParam,
  jsonTable,
  none,
  staticVal,
}

enum HttpMethod { get, post }

enum HttpPayload { string, json }
